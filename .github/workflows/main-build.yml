name: main-build-workflow
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup QEMU
        uses: docker/setup-qemu-action@v1

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to GitHub Docker Registry
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESSTOKEN }}

      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '^1.13.15'
      - run: |
          go version
          go get -u golang.org/x/lint/golint
      - name: build it
        run: |
          make dep
          make build

  deploy:
    runs-on: ubuntu-latest
    needs: [build]
    if: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
    steps:

      - name: Set versions properties
        run:  |
          AGENTVERSION=$(<agentversion.txt)
          echo "AGENTVERSION=$AGENTVERSION">> $GITHUB_ENV
          echo "AGENTVERSION: $AGENTVERSION"

      - name: Build and Deploy to Docker registry
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: |
            solaceiotteam/solace-axway-agent:alpha
            solaceiotteam/apim-demo-portal:${{env.AGENTVERSION}}